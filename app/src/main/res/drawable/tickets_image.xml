<!-- ?xml version="1.0" encoding="utf-8"? -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- android:state_pressed是被點擊 -->
    <!-- 默認時的背景圖片 -->
    <item android:state_selected="false" android:drawable="@drawable/tickets" />
    <item android:state_selected="true" android:drawable="@drawable/tickets_b" />
</selector>